'use client'

import {Template} from '@/components'

export default function NotFoundPage() {
    return (
        <Template>
            <h1 className='text-gray-900 text-center'>Page Not Found!</h1>
        </Template>
    )
}